# 该文件由 `kitten themes --config-file-name themes.conf` 维护。
# 切换主题时只修改本文件和 current-theme.conf，避免改写 kitty.conf。

# BEGIN_KITTY_THEME
# Konsole Breeze Dark.
# 加载当前选中的主题配色。
include current-theme.conf
# END_KITTY_THEME
