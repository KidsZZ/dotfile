# 标签栏与布局

# 仅启用自由分屏和单窗口放大两种高频布局。
enabled_layouts splits,stack
# 将标签栏显示在窗口顶部。
tab_bar_edge top
# 使用 Powerline 风格绘制标签栏。
tab_bar_style powerline
# 只有两个及以上标签时才显示标签栏。
tab_bar_min_tabs 2
# 关闭当前标签后返回最近使用的标签。
tab_switch_strategy previous
# 在标签标题前显示稳定的数字索引，便于快速定位。
tab_title_template "{index}: {title}"
