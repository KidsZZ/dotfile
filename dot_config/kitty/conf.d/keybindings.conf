# 快捷键

# 保留 Kitty 默认的 Ctrl+Shift 快捷键前缀。
kitty_mod ctrl+shift
# 新建分屏时继承当前窗口的工作目录。
map kitty_mod+enter new_window_with_cwd
# 新建标签时继承当前窗口的工作目录。
map kitty_mod+t new_tab_with_cwd
# 使用未被默认占用的 F9 快速放大或还原当前分屏。
map kitty_mod+f9 toggle_layout stack
