# Kitty 主配置只负责定义模块加载顺序，具体设置按职责拆分到 conf.d。

# 加载当前主题及其配色。
include themes.conf

# 加载字体配置。
include conf.d/fonts.conf
# 加载光标与窗口外观配置。
include conf.d/appearance.conf
# 加载终端交互行为配置。
include conf.d/behavior.conf
# 加载标签栏与窗口布局配置。
include conf.d/layouts.conf
# 加载自定义快捷键配置。
include conf.d/keybindings.conf
